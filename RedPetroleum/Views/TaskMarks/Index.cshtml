@model IEnumerable<RedPetroleum.Models.Entities.TaskMark>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mark.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TaskList.TaskName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MarkId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MarkValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Mark.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TaskList.TaskName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MarkId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MarkValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
